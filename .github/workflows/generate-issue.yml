name: Generate Lip Service Issue (DEPRECATED)
# This workflow used the old Python generator and is no longer active.
# All issue generation is handled by daily.yml via generate.js.
# Kept for reference only — do not trigger.

on:
  workflow_dispatch:
    inputs:
      _disabled:
        description: 'This workflow is deprecated. Use daily.yml instead.'
        required: false
        default: 'true'

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: |
          echo "This workflow is deprecated and does nothing."
          echo "All newsletter generation is handled by the daily.yml workflow."
          exit 0
